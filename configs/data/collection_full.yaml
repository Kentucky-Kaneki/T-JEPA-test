episodes_per_shard: 100
max_steps_per_episode: 50
red_policies: ["bline", "meander"]
blue_policies: ["sleep", "random", "coverage"]
# These are collection seeds, deliberately distinct from the five model-training
# seeds. They reproduce the immutable 18-shard Phase 3 data contract.
seeds: [1001, 2003, 3005]
shards_dir: data/shards
history_len: 4
train_ratio: 0.70
val_ratio: 0.15
test_ratio: 0.15
