phase: 5
seeds: [1001, 2003, 3005, 4007, 5009]
core:
  representation: flat
  hidden_dim: 64
  history_len: 4
  horizon: 8
  state_encoder_layers: 3
  state_encoder_ffn_dim: 256
  predictor_layers: 3
  predictor_ffn_dim: 256
  action_projection_depth: 2
  aggregator_mode: legacy_last_step_mean
  ema_momentum_init: 0.996
  ema_momentum_final: 1.0
  learning_rate: 0.001
  weight_decay: 0.0001
  batch_size: 64
  max_epochs: 20
  min_epochs: 10
  patience: 5
loss:
  variance_weight: 1.0
  covariance_weight: 0.04
  variance_target_std: 1.0
  action_weight: 0.0
  target_normalization: none
stage1:
  threshold_rule: train_median_rms_flat_observation_delta
  ratios:
    natural: null
    static75_dynamic25: [3, 1]
    static50_dynamic50: [1, 1]
    static25_dynamic75: [1, 3]
  primary_candidate: static50_dynamic50
stage2:
  action_weights: [0.0, 0.01, 0.05]
  initial_radius: 0.10
  future_margin: 0.20
  latent_margin: 0.20
stage3:
  baseline_selection: validation_best
  seeds: [1001, 2003, 3005]
  one_factor_at_a_time:
    latent_dim: [32]
    history_len: [2, 8]
    horizon: [4, 16]
    target_normalization: [batch_center]
